# Server Control Icon

`AppIcon.png` is newly generated artwork for this project, inspired by the
classic server's silver instrument enclosure and cyan/red/violet monitoring
palette. It does not contain extracted Bjango artwork. The transparent master
is retained here; `build-icon.sh` produces the standard 16px through 1024px macOS
representations and the build's `AppIcon.icns` without changing the master.
